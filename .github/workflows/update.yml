name: Update norsk feed

on:
  schedule:
    - cron: "0 * * * *"   # varje timme
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests lxml

      - name: Generate norsk feed
        run: |
          python generate_feed.py

      - name: Commit and push updated feed to gh-pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Byt till gh-pages branch, skapa om den inte finns
          if git show-ref --verify --quiet refs/heads/gh-pages; then
            git checkout gh-pages
          else
            git checkout --orphan gh-pages
            git rm -rf .
          fi

          # Lägg till mappen lampster-norge-feed med filen
          git add lampster-norge-feed/norsk-feed.xml

          # Commit och push
          git commit -m "Uppdaterad norsk feed [Automatiskt]" || echo "Inga ändringar att committa"
          git push origin gh-pages --force
